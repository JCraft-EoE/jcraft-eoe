/**
 * Allows addon developers to add their own options to the JCraft server config.
 * <p>
 * In order to create a config option, create an instance of one of the config option classes (or create your own)
 * sometime around the mod initialization. It's recommended (but not enforced) you create your own category for
 * the options your addon adds.
 */
package net.arna.jcraft.api.serverconfig;